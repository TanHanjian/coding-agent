# Tasks

- [x] 创建 `0002_question_memory.sql`，建立题目、标签、作答、复盘和附件表。
- [x] 创建题库领域枚举、实体、输入和搜索类型。
- [x] 创建 repository 接口和实现。
- [x] 创建 service 接口、错误定义和校验入口。
- [x] 创建 chi 路由注册和 HTTP handler。
- [x] 创建附件元数据服务接口和文件存储边界。
- [x] 创建 repository/service/handler 测试夹具入口。
- [x] 实现核心读写、搜索、分页和附件逻辑。

## 验收记录

- `backend`: `go test ./...` 通过。
- `web`: `npm run build` 通过。
- Step 2 已完成，待归档此 change。
